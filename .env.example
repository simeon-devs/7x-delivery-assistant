# Copy to .env and fill in. .env is gitignored and never committed.
ANTHROPIC_API_KEY=

# One password over the whole site (A-27). Leave it blank and there is no gate,
# which is what you want locally. Set it on anything with a public URL.
SEVENX_PASSWORD=
